:: Map [#map nostats]
<<set $GameState = "map">>

<<script>>
    if (State.variables.quest === undefined) {
        State.variables.canvas = {};
        State.variables.uuid = setup.uuid_v4();
        State.variables.quest = setup.maps.spiderQuest();
    }
        
    if (State.variables.map === undefined) {
        State.variables.map = new setup.map.Map({debug: true});
    }
<</script>>
