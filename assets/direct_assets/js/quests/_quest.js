// For the Quest Class.

// When we want to increment sequence, we just $(document).trigger(":sequenceupdated")
// Event listeners on the map will create new ones.

// When we switch map, we just new setup.map.Map('bgSrc'). That'll make the new map automatically, while properly disposing of the current map.
