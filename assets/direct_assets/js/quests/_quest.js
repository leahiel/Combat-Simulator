// For the Quest Class.

// When we want to increment sequence, we just $(document).trigger(":sequenceupdated")
// Event listeners on the map will create new ones.

// When we switch map, we just new setup.map.map('bgSrc'). That'll make the new map automatically.